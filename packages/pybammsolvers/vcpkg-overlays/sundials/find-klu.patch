diff --git a/cmake/tpl/FindKLU.cmake b/cmake/tpl/FindKLU.cmake
index 21db302..2619766 100644
--- a/cmake/tpl/FindKLU.cmake
+++ b/cmake/tpl/FindKLU.cmake
@@ -66,8 +66,13 @@ elseif(APPLE)
   set(CMAKE_FIND_LIBRARY_SUFFIXES d.a ${CMAKE_FIND_LIBRARY_SUFFIXES})
 endif()
 
-# Find include dir
-find_path(temp_KLU_INCLUDE_DIR klu.h ${KLU_INCLUDE_DIR})
+### Find include dir
+find_path(temp_KLU_INCLUDE_DIR NAMES klu.h
+  HINTS
+  "${_VCPKG_INSTALLED_DIR}/${VCPKG_TARGET_TRIPLET}"
+  PATH_SUFFIXES
+  include/suitesparse
+  )
 if(temp_KLU_INCLUDE_DIR)
   set(KLU_INCLUDE_DIR ${temp_KLU_INCLUDE_DIR})
 endif()
@@ -84,31 +89,48 @@ if(KLU_LIBRARY)
       CACHE PATH "" FORCE)
 
 else()
-  # find library with user provided directory path
-  set(KLU_LIBRARY_NAME klu)
-  find_library(KLU_LIBRARY ${KLU_LIBRARY_NAME} ${KLU_LIBRARY_DIR}
-               NO_DEFAULT_PATH)
+  ### Find KLU library
+  find_library(KLU_LIBRARY_DEBUG
+    NAMES klud
+    PATH_SUFFIXES lib
+    PATHS "${_VCPKG_INSTALLED_DIR}/${VCPKG_TARGET_TRIPLET}/debug"
+    NO_DEFAULT_PATH
+    )
+  find_library(KLU_LIBRARY_RELEASE
+    NAMES klu
+    PATH_SUFFIXES lib
+    PATHS "${_VCPKG_INSTALLED_DIR}/${VCPKG_TARGET_TRIPLET}"
+    NO_DEFAULT_PATH
+    )
+  include(SelectLibraryConfigurations)
+  select_library_configurations(KLU)
 endif()
 mark_as_advanced(KLU_LIBRARY)
 
+get_filename_component(KLU_LIBRARY_DIR_DEBUG ${KLU_LIBRARY_DEBUG} DIRECTORY)
+get_filename_component(KLU_LIBRARY_DIR_RELEASE ${KLU_LIBRARY_RELEASE} DIRECTORY)
+
 if(NOT AMD_LIBRARY)
   set(AMD_LIBRARY_NAME amd)
-  find_library(AMD_LIBRARY ${AMD_LIBRARY_NAME} ${KLU_LIBRARY_DIR}
-               NO_DEFAULT_PATH)
+  find_library(AMD_LIBRARY_DEBUG ${AMD_LIBRARY_NAME}d ${KLU_LIBRARY_DIR_DEBUG} NO_DEFAULT_PATH)
+  find_library(AMD_LIBRARY_RELEASE ${AMD_LIBRARY_NAME} ${KLU_LIBRARY_DIR_RELEASE} NO_DEFAULT_PATH)
+  select_library_configurations(AMD)
   mark_as_advanced(AMD_LIBRARY)
 endif()
 
 if(NOT COLAMD_LIBRARY)
   set(COLAMD_LIBRARY_NAME colamd)
-  find_library(COLAMD_LIBRARY ${COLAMD_LIBRARY_NAME} ${KLU_LIBRARY_DIR}
-               NO_DEFAULT_PATH)
+  find_library(COLAMD_LIBRARY_DEBUG ${COLAMD_LIBRARY_NAME}d ${KLU_LIBRARY_DIR_DEBUG} NO_DEFAULT_PATH)
+  find_library(COLAMD_LIBRARY_RELEASE ${COLAMD_LIBRARY_NAME} ${KLU_LIBRARY_DIR_RELEASE} NO_DEFAULT_PATH)
+  select_library_configurations(COLAMD)
   mark_as_advanced(COLAMD_LIBRARY)
 endif()
 
 if(NOT BTF_LIBRARY)
   set(BTF_LIBRARY_NAME btf)
-  find_library(BTF_LIBRARY ${BTF_LIBRARY_NAME} ${KLU_LIBRARY_DIR}
-               NO_DEFAULT_PATH)
+  find_library(BTF_LIBRARY_DEBUG ${BTF_LIBRARY_NAME}d ${KLU_LIBRARY_DIR_DEBUG} NO_DEFAULT_PATH)
+  find_library(BTF_LIBRARY_RELEASE ${BTF_LIBRARY_NAME} ${KLU_LIBRARY_DIR_RELEASE} NO_DEFAULT_PATH)
+  select_library_configurations(BTF)
   mark_as_advanced(BTF_LIBRARY)
 endif()
 
@@ -118,13 +140,15 @@ if(NOT SUITESPARSECONFIG_LIBRARY)
   if(MSVC OR ("${CMAKE_C_SIMULATE_ID}" STREQUAL "MSVC"))
     set(CMAKE_FIND_LIBRARY_PREFIXES "")
   endif()
-  find_library(SUITESPARSECONFIG_LIBRARY ${SUITESPARSECONFIG_LIBRARY_NAME}
-               ${KLU_LIBRARY_DIR} NO_DEFAULT_PATH)
+  find_library(SUITESPARSECONFIG_LIBRARY_DEBUG ${SUITESPARSECONFIG_LIBRARY_NAME}d ${KLU_LIBRARY_DIR_DEBUG} NO_DEFAULT_PATH)
+  find_library(SUITESPARSECONFIG_LIBRARY_RELEASE ${SUITESPARSECONFIG_LIBRARY_NAME} ${KLU_LIBRARY_DIR_RELEASE} NO_DEFAULT_PATH)
+  select_library_configurations(SUITESPARSECONFIG)
   mark_as_advanced(SUITESPARSECONFIG_LIBRARY)
 endif()
 
-set(KLU_LIBRARIES ${KLU_LIBRARY} ${AMD_LIBRARY} ${COLAMD_LIBRARY}
-                  ${BTF_LIBRARY} ${SUITESPARSECONFIG_LIBRARY})
+set(KLU_LIBRARIES_DEBUG ${KLU_LIBRARY_DEBUG} ${AMD_LIBRARY_DEBUG} ${COLAMD_LIBRARY_DEBUG} ${BTF_LIBRARY_DEBUG} ${SUITESPARSECONFIG_LIBRARY_DEBUG})
+set(KLU_LIBRARIES_RELEASE ${KLU_LIBRARY_RELEASE} ${AMD_LIBRARY_RELEASE} ${COLAMD_LIBRARY_RELEASE} ${BTF_LIBRARY_RELEASE} ${SUITESPARSECONFIG_LIBRARY_RELEASE})
+set(KLU_LIBRARIES ${KLU_LIBRARY} ${AMD_LIBRARY} ${COLAMD_LIBRARY} ${BTF_LIBRARY} ${SUITESPARSECONFIG_LIBRARY})
 
 # set package variables including KLU_FOUND
 find_package_handle_standard_args(KLU REQUIRED_VARS KLU_LIBRARY KLU_LIBRARIES
@@ -139,8 +163,10 @@ if(KLU_FOUND)
 
   set_target_properties(
     SUNDIALS::KLU
-    PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${KLU_INCLUDE_DIR}"
-               INTERFACE_LINK_LIBRARIES "${KLU_LIBRARIES}"
-               IMPORTED_LOCATION "${KLU_LIBRARY}")
+    PROPERTIES
+      INTERFACE_INCLUDE_DIRECTORIES "${KLU_INCLUDE_DIR}"
+      INTERFACE_LINK_LIBRARIES "$<IF:$<CONFIG:DEBUG>,${KLU_LIBRARIES_DEBUG},${KLU_LIBRARIES_RELEASE}>"
+      IMPORTED_LOCATION_DEBUG "${KLU_LIBRARY_DEBUG}"
+      IMPORTED_LOCATION_RELEASE "${KLU_LIBRARY_RELEASE}")
 
 endif()
